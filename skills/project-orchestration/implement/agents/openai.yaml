interface:
  display_name: "Implement"
  short_description: "Execute approved issues through tested commits"
  default_prompt: "Use $implement to execute the approved issue graph autonomously through tested commits."
policy:
  allow_implicit_invocation: false
